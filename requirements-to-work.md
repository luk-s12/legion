# Story <ID-1>

(Replace with the real description of the first User Story to work on. You can
write it yourself following the format below, use `/new-story` so the assistant helps
you refine it against the base repo's real code, or use `/new-objective` if you don't yet
know how many stories the thing you want to achieve implies — it splits a high-level objective into several
stories like this one.)

## Acceptance criteria
- ...

## Definitions taken
- ...

## Estimated impact zone
- ...

## Depends on (optional)
(Only if this story doesn't make sense before another one is already `finalized` — for a business
reason, not a code one (that's already handled by automatic overlap detection). Format: `- <ID>`.
If not applicable, delete this section.)

## Subtasks (optional)
(Only if the story combines domains with a categorically distinct approval criterion — e.g.
one part needs a security sign-off separate from the code. Format:
`N. [domain] Description (depends on M)`. If not applicable, delete this section — `/legion`
assigns the generalist agent end to end by default.)

---

# Story <ID-2>

(Replace with the second story. There is no limit on the number of stories — the system creates a
`git worktree` for each one on the same base repo; if two overlap, `/legion`
automatically serializes them into different batches instead of running them together.)

## Acceptance criteria
- ...

## Definitions taken
- ...

## Estimated impact zone
- ...

## Depends on (optional)
- ...

## Subtasks (optional)
- ...
