# Lessons learned

Incidents (in production or any other environment) caused by a business rule that wasn't
accounted for during implementation. Kept as permanent history across runs — it never
resets. Consulted by `research-agent` (prior-research mode) and by the
analysis of `/new-story`, filtering by Zone, before designing any new story on the
same part of the code.

Added with the `/new-lesson` command whenever something breaks (in any environment) due to a
rule that wasn't accounted for. No lessons yet.
